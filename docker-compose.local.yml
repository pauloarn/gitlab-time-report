# Local development only — publishes a host port for direct browser access.
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
services:
  gitlab-time-report:
    ports:
      - "${HOST_PORT:-8080}:3000"
